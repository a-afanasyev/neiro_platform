# 🚀 Быстрый старт Neiro Platform

## ✅ Фаза 0 завершена!

Базовая инфраструктура проекта развернута и готова к работе.

### Что уже работает:

- ✅ Docker Compose конфигурация
- ✅ PostgreSQL 15 (порт **5437**)
- ✅ Redis 7 (порт **6380**)
- ✅ MinIO S3-совместимое хранилище (порты **9000, 9001**)
- ✅ Adminer - UI для управления БД (порт **8082**)
- ✅ Node.js 20 контейнер для разработки
- ✅ Структура монорепозитория (apps, packages, services)
- ✅ Next.js 14 приложение
- ✅ Prisma схема БД (27 таблиц)
- ✅ Shared пакеты: @neiro/types, @neiro/utils, @neiro/database
- ✅ CI/CD конфигурация (GitHub Actions)

## 🐳 Запуск инфраструктуры

```bash
# 1. Перейти в директорию проекта
cd nero_platform

# 2. Запустить все контейнеры
docker-compose up -d

# 3. Проверить статус
docker-compose ps

# Все контейнеры должны быть в статусе "Up" или "healthy"
```

## 📦 Установка зависимостей

```bash
# Установить зависимости (в контейнере app)
docker-compose exec app pnpm install
```

## 🗄️ Настройка базы данных

```bash
# Генерация Prisma Client
docker-compose exec app pnpm run db:generate

# Применение миграций
docker-compose exec app pnpm run db:migrate

# Заполнение тестовыми данными
docker-compose exec app pnpm run db:seed
```

## 🚀 Запуск приложения

```bash
# Запуск Next.js в режиме разработки
docker-compose exec app pnpm --filter @neiro/web dev
```

Приложение будет доступно по адресу: http://localhost:3001

## 📊 Доступ к сервисам

| Сервис | URL | Логин | Пароль |
|--------|-----|-------|--------|
| **Next.js Frontend** | http://localhost:3001 | - | - |
| **Adminer (DB UI)** | http://localhost:8082 | Server: `postgres`<br>User: `neiro_user`<br>DB: `neiro_platform` | `neiro_password_dev` |
| **MinIO Console** | http://localhost:9001 | `minioadmin` | `minioadmin123` |
| **PostgreSQL** | localhost:5437 | `neiro_user` | `neiro_password_dev` |
| **Redis** | localhost:6380 | - | - |

## 💻 Разработка

### Запуск команд в контейнере

Все команды выполняются внутри контейнера через `docker-compose exec app`:

```bash
# Установка нового пакета
docker-compose exec app pnpm add <package>

# Линтинг
docker-compose exec app pnpm lint

# Форматирование кода
docker-compose exec app pnpm format

# Проверка типов
docker-compose exec app pnpm type-check

# Работа с БД
docker-compose exec app pnpm run db:studio  # Prisma Studio
docker-compose exec app pnpm run db:push    # Push схемы без миграций
```

### Редактирование кода

Редактируйте файлы на хост-машине в любимом редакторе. Изменения автоматически синхронизируются с контейнером благодаря volume mount.

## 🛑 Остановка и очистка

```bash
# Остановить контейнеры
docker-compose down

# Остановить и удалить volumes (⚠️ удалит данные БД!)
docker-compose down -v

# Перезапустить определенный контейнер
docker-compose restart app
```

## 📋 Тестовые пользователи

После выполнения `pnpm run db:seed` будут созданы:

### Пользователи

- **Admin**: admin@neiro.dev
- **Супервизор**: supervisor@neiro.dev
- **Нейропсихолог**: neuro@neiro.dev
- **Логопед**: speech@neiro.dev
- **ABA-терапевт**: aba@neiro.dev
- **Родитель 1**: parent1@example.com
- **Родитель 2**: parent2@example.com

### Дети

- Артем Иванов (5 лет, РАС средней степени)
- София Петрова (4 года, РАС легкой степени)

### Упражнения

- Сортировка по цветам
- Повторение звуков
- Пальчиковая гимнастика

## 🚨 Проблемы и решения

### Порты заняты

Если порты заняты локальными сервисами:
- PostgreSQL: изменен с 5432 на **5437**
- Redis: изменен с 6379 на **6380**
- Adminer: изменен с 8080 на **8082**
- Next.js: изменен с 3000 на **3001**

### Контейнер не запускается

```bash
# Проверить логи
docker-compose logs app

# Пересобрать контейнер
docker-compose build --no-cache app
docker-compose up -d app
```

### Ошибки pnpm

```bash
# Очистить node_modules и переустановить
docker volume rm nero_platform_node_modules
docker-compose up -d app
docker-compose exec app pnpm install
```

## 📚 Следующие шаги

После настройки инфраструктуры:

1. **Месяц 1**: Разработка Auth, Users, Specialists, Children, базовая Diagnostics
2. **Месяц 2**: Route Orchestrator, Exercises, Templates, Assignments
3. **Месяц 3**: Reports, Media, Communications, Analytics, PWA

Подробный роадмап см. в `neiro-platform-implementation.plan.md`

## 🤝 Команда разработки

При возникновении вопросов обращайтесь к Tech Lead.

---

**Статус**: ✅ Фаза 0 завершена  
**Дата**: 14 ноября 2025  
**Версия**: 0.1.0

