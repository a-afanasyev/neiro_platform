# E2E тесты Neiro Platform

Этот каталог содержит End-to-End (E2E) тесты для платформы Neiro, написанные с использованием Playwright.

## 📁 Структура тестов

```
e2e/
├── auth.spec.ts          # Тесты аутентификации и авторизации
├── dashboard.spec.ts     # Тесты основного dashboard
├── cjm.spec.ts          # Тесты Customer Journey Map сценариев
└── README.md            # Этот файл
```

## 🎯 Покрытие тестами

### auth.spec.ts
- ✅ Отображение страницы логина
- ✅ Успешный вход в систему
- ✅ Навигация на страницу регистрации
- ✅ Выход из системы
- ✅ Сохранение сессии после перезагрузки страницы
- ✅ Редирект на логин при доступе к защищенным страницам без авторизации

### dashboard.spec.ts
- ✅ Базовая навигация по dashboard
- ✅ Доступ к различным разделам (Дети, Диагностика, Маршруты и т.д.)

### cjm.spec.ts (Customer Journey Map)

#### CJM #1: Родитель - Онбординг
- ✅ Вход родителя в систему после приглашения
- ✅ Просмотр списка своих детей
- ✅ Просмотр профиля ребенка

#### CJM #2: Родитель - Выполнение программы
- ✅ Просмотр назначенных упражнений
- ✅ Просмотр календаря занятий
- ✅ Просмотр деталей упражнения
- ✅ Отметка выполнения назначения

#### CJM #3: Нейропсихолог - Полный цикл
- ✅ Вход специалиста в систему
- ✅ Просмотр списка закрепленных детей
- ✅ Создание диагностической сессии
- ✅ Просмотр доступных опросников
- ✅ Создание индивидуального маршрута
- ✅ Просмотр существующих маршрутов
- ✅ Создание назначения для ребенка
- ✅ Просмотр библиотеки упражнений
- ✅ Просмотр шаблонов программ

#### Сквозные сценарии
- ✅ Полный цикл работы специалиста
- ✅ Просмотр прогресса выполнения программы

**Всего:** 23+ автоматизированных E2E теста

## 🚀 Запуск тестов

### Предварительные требования

1. Убедитесь, что backend сервисы и база данных запущены:
   ```bash
   # В корне проекта
   docker-compose up -d
   
   # Или используйте скрипт
   ./scripts/start-services.sh
   ```

2. Убедитесь, что база данных заполнена тестовыми данными:
   ```bash
   # В корне проекта
   cd packages/database
   pnpm exec prisma db seed
   ```

3. Убедитесь, что frontend приложение запущено:
   ```bash
   cd apps/web
   pnpm dev
   ```

### Команды для запуска тестов

```bash
# Перейти в директорию frontend
cd apps/web

# Запустить все E2E тесты
pnpm test:e2e

# Запустить тесты в headless режиме (без UI)
pnpm exec playwright test

# Запустить тесты в UI режиме (для отладки)
pnpm exec playwright test --ui

# Запустить конкретный файл с тестами
pnpm exec playwright test auth.spec.ts
pnpm exec playwright test cjm.spec.ts

# Запустить конкретный тест по имени
pnpm exec playwright test -g "CJM #1"
pnpm exec playwright test -g "should login successfully"

# Запустить тесты в debug режиме
pnpm exec playwright test --debug

# Запустить тесты только в Chromium
pnpm exec playwright test --project=chromium

# Запустить тесты с отчетом
pnpm exec playwright test --reporter=html
```

### Просмотр отчетов

После запуска тестов с флагом `--reporter=html`, отчет можно открыть:

```bash
pnpm exec playwright show-report
```

## 🧪 Тестовые данные

Все E2E тесты используют тестовые данные из `packages/database/prisma/seed.ts`:

### Пользователи

| Email | Пароль | Роль | Описание |
|-------|--------|------|----------|
| admin@neiro.dev | admin123 | admin | Администратор системы |
| parent1@example.com | parent123 | parent | Родитель (Иван Иванов) |
| parent2@example.com | parent123 | parent | Родитель (Мария Петрова) |
| specialist1@example.com | specialist123 | specialist | Нейропсихолог (Анна Смирнова) |
| specialist2@example.com | specialist123 | specialist | Логопед (Елена Кузнецова) |
| specialist3@example.com | specialist123 | specialist | ABA-терапевт (Дмитрий Соколов) |

### Дети

- **Алиса Иванова** (5 лет) - ребенок parent1, закреплен за specialist1, specialist2, specialist3
- **Борис Петров** (7 лет) - ребенок parent1, закреплен за specialist1, specialist2
- **Вера Сидорова** (4 года) - ребенок parent2, закреплена за specialist1
- **Глеб Морозов** (6 лет) - ребенок parent2, закреплен за specialist2

### Опросники

- MCHAT-R (Ранний скрининг аутизма)
- CARS-2 (Шкала оценки аутизма)
- ADOS-2 (Диагностическое наблюдение)
- ADI-R (Диагностическое интервью)
- Vineland-3 (Адаптивное поведение)
- BRIEF-2 (Исполнительные функции)

## 🔧 Отладка тестов

### Использование Playwright Inspector

```bash
# Запуск с инспектором
pnpm exec playwright test --debug

# Запуск конкретного теста с инспектором
pnpm exec playwright test cjm.spec.ts --debug -g "CJM #1.1"
```

### Использование Trace Viewer

```bash
# Запуск с записью trace
pnpm exec playwright test --trace on

# Просмотр trace после выполнения
pnpm exec playwright show-trace trace.zip
```

### Скриншоты и видео

По умолчанию Playwright сохраняет скриншоты и видео только для упавших тестов в директории `test-results/`.

Для включения записи для всех тестов, отредактируйте `playwright.config.ts`:

```typescript
use: {
  screenshot: 'on',  // 'on' | 'off' | 'only-on-failure'
  video: 'on',       // 'on' | 'off' | 'retain-on-failure'
}
```

## 📝 Написание новых тестов

### Шаблон базового теста

```typescript
import { test, expect } from '@playwright/test'

test.describe('Название группы тестов', () => {
  test.beforeEach(async ({ page }) => {
    // Подготовка перед каждым тестом
    await page.goto('/')
  })

  test('Описание теста', async ({ page }) => {
    // Шаги теста
    await page.click('button')
    await expect(page.locator('text=Результат')).toBeVisible()
  })
})
```

### Использование вспомогательных функций

В `cjm.spec.ts` определены вспомогательные функции, которые можно переиспользовать:

```typescript
// Очистка storage
await clearStorage(page)

// Вход в систему
await loginAs(page, 'parent1@example.com', 'parent123')
```

### Best Practices

1. **Используйте data-testid для стабильных селекторов:**
   ```typescript
   await page.locator('[data-testid="submit-button"]').click()
   ```

2. **Используйте явные ожидания:**
   ```typescript
   await expect(page.locator('text=Загрузка...')).toBeVisible({ timeout: 5000 })
   ```

3. **Группируйте связанные тесты:**
   ```typescript
   test.describe('Feature Name', () => {
     // Связанные тесты
   })
   ```

4. **Очищайте состояние перед каждым тестом:**
   ```typescript
   test.beforeEach(async ({ page }) => {
     await clearStorage(page)
   })
   ```

5. **Используйте Page Object Model для сложных страниц:**
   ```typescript
   class LoginPage {
     constructor(private page: Page) {}
     
     async login(email: string, password: string) {
       await this.page.fill('[name="email"]', email)
       await this.page.fill('[name="password"]', password)
       await this.page.click('button[type="submit"]')
     }
   }
   ```

## 🐛 Распространенные проблемы

### Тесты падают с timeout

**Решение:** Увеличьте timeout в `playwright.config.ts` или для конкретного теста:

```typescript
test('slow test', async ({ page }) => {
  test.setTimeout(60000) // 60 секунд
  // ...
})
```

### Тесты падают из-за localStorage

**Решение:** Используйте функцию `clearStorage()` с обработкой ошибок (уже реализовано в `cjm.spec.ts`).

### Backend не отвечает

**Решение:** Убедитесь, что все сервисы запущены:

```bash
docker-compose ps
# Все сервисы должны быть в статусе "Up"
```

### База данных пустая

**Решение:** Выполните seed:

```bash
cd packages/database
pnpm exec prisma db seed
```

## 📚 Дополнительные ресурсы

- [Playwright Documentation](https://playwright.dev/)
- [Playwright Best Practices](https://playwright.dev/docs/best-practices)
- [Playwright API Reference](https://playwright.dev/docs/api/class-playwright)
- [CJM Documentation](../../Documents/CJM_MANUAL_TEST_READINESS.md)
- [Testing Report](../../Documents/TESTING_REPORT.md)

## 🤝 Вклад в тесты

При добавлении новых функций в платформу, пожалуйста:

1. Добавьте соответствующие E2E тесты
2. Убедитесь, что все существующие тесты проходят
3. Обновите этот README при необходимости
4. Добавьте тестовые данные в `seed.ts` если требуется

## 📊 CI/CD Integration

E2E тесты могут быть интегрированы в CI/CD pipeline:

```yaml
# Пример для GitHub Actions
- name: Run E2E tests
  run: |
    cd apps/web
    pnpm exec playwright test
  env:
    CI: true
```

Для production окружения рекомендуется запускать E2E тесты:
- При каждом pull request
- Перед деплоем на staging/production
- По расписанию (например, ночью) для smoke тестов

