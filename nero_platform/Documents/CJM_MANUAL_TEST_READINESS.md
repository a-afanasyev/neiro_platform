# 🧭 Neiro Platform — CJM и готовность к ручному тестированию

**Дата:** 16 ноября 2025  
**Версия документа:** 1.0  
**Источник данных:** `PROJECT_CONTEXT.md`, `BUSINESS_PROCESSES_AND_WORKFLOWS.md`, `Neiro_CJM_Extended.md`, `MIGRATION_AND_TESTING_REPORT.md`, `TESTING_REPORT.md`

---

## 1. Общее состояние платформы

- ✅ Микросервисная архитектура развернута (8 сервисов + фронт + инфраструктура).
- ✅ Все сервисы проходят health-check (`/health`).
- ✅ Больше 95% API эндпоинтов отработали успешно в функциональных тестах (19 из 20).
- ⚠️ Есть **две ключевые проблемы**, влияющие на CJM:
  - `GET /children/v1` → 500 (невозможно корректно получить список детей).
  - Страница фронтенда `/dashboard/routes` ломается из‑за отсутствующего компонента `@/components/ui/badge`.
- ✅ E2E фронтенда частично пройдены: логин и базовый dashboard работают, навигация для `admin` проверена.

В этом документе перечислены CJM из `Neiro_CJM_Extended.md` и указано, **какие шаги реально можно покрыть ручными сценариями уже сейчас**, а где нужны доработки.

---

## 2. CJM #1 — Родитель: первый контакт и онбординг

См. `Neiro_CJM_Extended.md` → **CJM #1. Родитель — первый контакт и онбординг**.

### Что уже можно тестировать руками

- **Авторизация (часть онбординга):**
  - Логин через страницу `/login`:
    - ✅ Вход под `admin@neiro.dev / admin123` (проверено E2E).
    - ✅ Редирект на `/dashboard` после успешного входа.
  - Проверка базового RBAC на фронте:
    - ✅ ProtectedRoute отсекает неавторизованных пользователей (по тестам и коду).
- **Навигация после входа:**
  - Для роли `admin` доступны разделы dashboard:
    - Главная / Overview.
    - Пользователи.
    - Дети.
    - Маршруты (⚠️ страница падает).
    - Диагностика.
    - Упражнения.
    - Шаблоны.
    - Назначения.

### Ограничения и пробелы

- ⚠️ **Регистрация родителя через UI отсутствует**:
  - Регистрация реализована через invite (`POST /auth/v1/invite`), а не через публичную форму.
  - CJM шаги «регистрация» / «первичный скрининг» можно тестировать **только частично** через API (без полноценного UX для родителя).
- ⚠️ Нет готового UI для **скрининговых опросников** на фронте (они реализованы на уровне API `diagnostics`, но не как полноценный родительский интерфейс).

**Итог по CJM #1:**  
Для ручной проверки сейчас доступны: **логин**, базовая навигация, работа ролей. Полный онбординг родителя (регистрация + скрининг) требует доработки фронта и сценариев.

---

## 3. CJM #2 — Родитель: выполнение программы дома

См. `Neiro_CJM_Extended.md` → **CJM #2. Родитель — выполнение программы дома**.

### Бэкенд‑готовность

- `Assignments Service` (порт 4006):
  - ✅ `GET /assignments/v1` — список назначений.
  - ✅ `PATCH /assignments/v1/:id/status` — изменение статуса назначения.
  - ✅ `GET /assignments/v1/calendar` — данные для календаря.
- `Exercises Service` (порт 4007):
  - ✅ `GET /exercises/v1` — список упражнений.
  - ✅ `GET /exercises/v1/categories` — категории упражнений.
  - ✅ `POST /exercises/v1` / `POST /exercises/v1/:id/publish` — управление библиотекой упражнений.

Это означает, что **серверная часть для назначения и выполнения упражнений, а также календаря — в основном работает**.

### Что можно тестировать руками уже сейчас

- Через API (Postman / curl):
  - Создание упражнения, его публикация.
  - Создание назначений (assignments) и изменение их статуса (`scheduled → in_progress → completed`).
  - Получение календаря назначений.
- Через фронтенд:
  - ⚠️ Формально есть разделы dashboard для:
    - Упражнений (`/dashboard/exercises`).
    - Назначений (`/dashboard/assignments`).
  - По результатам E2E эти экраны **еще не проходили полное тестирование**, но навигация до них для `admin` работает.

### Ограничения

- Нет законченного «родительского» UI (PWA/Telegram) для:
  - Запуска упражнения с таймером.
  - Формирования отчета с медиа.
  - Флоу «быстрый отчет с телефона».

**Итог по CJM #2:**  
Можно полноценно проверять **бэкенд‑логику** заданий и упражнений (через API) и базовую навигацию по соответствующим разделам dashboard. Полный UX для родителя (мобильный сценарий) пока не реализован.

---

## 4. CJM #3 — Нейропсихолог: полный цикл сопровождения

См. `Neiro_CJM_Extended.md` → **CJM #3. Нейропсихолог — полный цикл сопровождения**.  
Связан с бизнес‑процессами BP‑02 (Диагностика) и BP‑03/BP‑04/BP‑07 (Маршрут, назначения, ревизия).

### Что уже работает на уровне API

- `Diagnostics Service` (порт 4004):
  - ✅ Список опросников: `GET /diagnostics/v1/questionnaires`.
  - ✅ Создание диагностической сессии: `POST /diagnostics/v1/sessions`.
  - ✅ Получение/обновление сессий, ответы, завершение: `GET/PUT/POST /diagnostics/v1/sessions/...`.
- `Routes Service` (порт 4005):
  - ✅ Создание маршрута: `POST /routes/v1`.
  - ✅ Список маршрутов, получение детали, изменение: `GET /routes/v1`, `GET /routes/v1/:id`, `PATCH /routes/v1/:id`.
  - ✅ Смена статуса: `POST /routes/v1/:id/activate`, `POST /routes/v1/:id/complete`.
- `Templates Service` (порт 4008):
  - ✅ CRUD‑операции над шаблонами маршрутов.

### Что можно тестировать руками

- На API‑уровне:
  - Полный сценарий: **диагностическая сессия → рекомендации → создание маршрута → работа с шаблонами → активация маршрута**.
  - Проверка бизнес‑правил (Constitution Check) по структуре маршрута (фазы, цели, упражнения).

- На фронтенде:
  - Навигация к разделам:
    - Диагностика (`/dashboard/diagnostics`, название может отличаться, но раздел есть).
    - Маршруты (`/dashboard/routes`) — ⚠️ **страница падает из‑за отсутствующего `badge`**.

### Ограничения

- ⚠️ Из‑за ошибки `@/components/ui/badge` нельзя закончить ручное тестирование **UI конструктора маршрутов**.
- UI для создания диагностических сессий и работы с шаблонами реализован частично; основной уровень готовности сейчас — **API**.

**Итог по CJM #3:**  
Бэкенд‑цепочка диагностический цикл → маршруты → назначения **готова и протестирована API‑тестами**. Для ручного UX‑тестирования нейропсихолога нужен быстрый фикс страницы маршрутов (добавить/заменить компонент `badge`) и добрать фронтовые формы.

---

## 5. CJM #4–#6 — Профильный специалист, супервизор, администратор

См. `Neiro_CJM_Extended.md` → **CJM #4–#6**.

Эти CJM в основном накладываются на уже описанные модули:

- Профильный специалист:
  - Работает с **назначениями**, **упражнениями**, **отчетами**, **вехами маршрута**.
  - На уровне API это сейчас покрывается `assignments`, `exercises`, частично `routes`.
- Супервизор:
  - Использует **аналитику**, **аудит**, **просмотр маршрутов и отчетов**.
  - Аналитика/репорты пока описаны в модели, но UI и отдельные сервисы аналитики еще не выделены.
- Администратор/организация:
  - Управляет **пользователями и ролями**, **специалистами**, **организациями**:
    - `Auth Service`: login, invite, logout.
    - `Users Service`: `GET /users/v1`, `GET/PUT/DELETE /users/v1/:id`, список специалистов.

### Что можно тестировать руками уже сейчас

- Через фронтенд (`admin` роль):
  - Навигацию по разделам dashboard.
  - Просмотр пользователей (UI есть, API работает).
- Через API:
  - CRUD по пользователям (Users Service).
  - Invite‑флоу для создания новых пользователей (Auth Service).

### Ограничения

- Нет выделенного UI для:
  - Супервизорских дашбордов и отчетов.
  - Управления лицензиями специалистов.
  - Полноценного орга‑уровня (мультитенантность, организации).

**Итог:**  
Эти CJM **частично покрываются** уже работающими API и базовым admin‑dashboard, но полноценные «роли‑ориентированные» интерфейсы еще впереди.

---

## 6. CJM #7–#12 — Детский UX, Telegram/PWA, поддержка, обучение

См. `Neiro_CJM_Extended.md` → **CJM #7–#12**.

Эти сценарии описывают в основном:

- Игровой интерфейс ребенка.
- Быстрые взаимодействия через Telegram/PWA.
- Поддержку, обучение, службы помощи.

### Текущий статус

- Большая часть этих CJM относится к **будущим фронт‑модулям и внешним интеграциям**:
  - Нет реализованного игрового интерфейса для ребенка.
  - Telegram Bot, WebApp и PWA‑оболочка на данный момент не представлены в кодовой базе `nero_platform`.
  - Поддержка и обучение (база знаний, чат‑поддержка, AI‑помощник) пока только в требованиях.

**Итог:**  
CJM #7–#12 **еще не имеют технической реализации в текущем репозитории** и не могут быть полноценно проверены вручную. Они выступают как ориентир для следующих итераций разработки.

---

## 7. Сводная таблица по CJM и готовности к ручному тестированию

| CJM | Название (кратко) | Основные модули | Статус ручного тестирования |
|-----|-------------------|-----------------|------------------------------|
| #1 | Родитель — онбординг | Auth, Users, Diagnostics (частично) | 🟡 Частично: логин + dashboard, нет публичной регистрации и UI скрининга |
| #2 | Родитель — выполнение дома | Assignments, Exercises | 🟡 Бэкенд готов (API), UI родителя и PWA не реализованы |
| #3 | Нейропсихолог — сопровождение | Diagnostics, Routes, Templates, Assignments | 🟡 API‑цепочка работает, UI маршрутов ломается, UX неполный |
| #4 | Профильный специалист | Assignments, Exercises, Reports | 🟡 Можно тестировать API и часть admin‑UI, специализированного UI нет |
| #5 | Супервизор | Analytics, Routes, Reports | 🔴 Нет реализованных аналитических UI, только база для маршрутов |
| #6 | Администратор/организация | Auth, Users, RBAC | 🟡 Базовый admin‑dashboard и API работают, org‑уровень не завершен |
| #7 | Ребенок — игровой опыт | Игровой UI упражнений | 🔴 Не реализован |
| #8 | Быстрые взаимодействия Telegram/PWA | Comms, PWA, Telegram | 🔴 Не реализованы в текущем репо |
| #9–#12 | Проф/диагностика, пост‑диагностика, поддержка | Смешанные модули | 🔴 В основном на уровне требований/дизайна |

---

## 8. Рекомендованный набор ручных сценариев «здесь и сейчас»

Ниже — минимальный список **реально исполняемых** ручных сценариев, который имеет смысл прогонять уже сейчас.

### 8.1. Базовый сценарий авторизации и навигации (CJM #1, ядро)

1. Запустить инфраструктуру и все сервисы (см. `README.md` / `PROJECT_CONTEXT.md`).
2. Открыть `http://localhost:3001/login`.
3. Войти под `admin@neiro.dev / admin123`.
4. Убедиться, что происходит редирект на `/dashboard`.
5. Проверить доступность основных пунктов меню:
   - Главная, Пользователи, Дети, Диагностика, Упражнения, Шаблоны, Назначения.
6. Перейти на каждую страницу и зафиксировать:
   - Загружается ли страница без ошибок.
   - Нет ли ошибок в консоли браузера.

### 8.2. Проверка API детей, пользователей, упражнений, назначений (CJM #1–#4)

Через Postman/curl:

- `Auth`:
  - `POST /auth/v1/login` для получения пары access/refresh токенов.
- `Users`:
  - `GET /users/v1` — список пользователей.
- `Children`:
  - `GET /children/v1` — **ожидаемый 500** (зафиксировать лог и поведение).
- `Exercises`:
  - `GET /exercises/v1`, `GET /exercises/v1/categories`.
- `Assignments`:
  - `GET /assignments/v1`, `PATCH /assignments/v1/:id/status`.

### 8.3. Диагностика и маршруты (CJM #3, ядро)

- `Diagnostics`:
  - Создать диагностическую сессию.
  - Добавить ответы и завершить ее.
- `Routes`:
  - Создать маршрут через `POST /routes/v1`.
  - Активировать его.
  - Получить список маршрутов.
- На фронте:
  - Попробовать открыть страницу маршрутов (зафиксировать падение из‑за `badge`).

---

## 9. Следующие шаги для улучшения покрываемости CJM

1. **Исправить критические блокеры:**
   - Добавить/реализовать компонент `@/components/ui/badge` или вырезать его использование на странице маршрутов.
   - Исправить 500‑ошибку в `GET /children/v1`.
2. **Для CJM #1:**
   - Добавить публичную регистрацию для роли `parent`.
   - Реализовать базовый UI первичного скрининга (на базе уже готовых diagnostics API).
3. **Для CJM #2:**
   - Собрать простой родительский интерфейс: список назначений + выполнение + отправка отчета.
4. **Для CJM #3:**
   - Завершить UI для конструктора маршрутов и просмотра диагностических сессий.
5. **Для CJM #4–#6:**
   - Добавить минимальные дашборды для специалистов и супервизоров (фильтрация по маршрутам/назначениям).
6. **Для CJM #7–#12:**
   - Спланировать отдельный инкремент, так как это новые фронт‑модули и интеграции (Telegram, PWA, поддержка).

---

**Статус документа:**  
✅ Актуален на момент миграции на микросервисную архитектуру (16.11.2025).  
Рекомендуется обновлять после каждого крупного релиза или изменения CJM/функциональности.

---

## 10. Автотесты и CI

### 10.1. Запуск тестов локально

- **Unit/Integration Backend**  
  - Установить зависимости в корне: `pnpm install`.  
  - Запуск всех тестов монорепы: `pnpm test` (под капотом `turbo run test`).  
  - По сервисам:  
    - Auth/Exercises/Templates/Routes/Assignments (Jest): `pnpm --filter @neiro/auth test`, `pnpm --filter @neiro/exercises test` и т.п.  
    - Children/Users (Vitest): `pnpm --filter @neiro/children-service test`, `pnpm --filter @neiro/users-service test`.

- **Frontend E2E (Playwright)**  
  - Перейти в `apps/web`: `cd apps/web`.  
  - Убедиться, что backend и БД запущены (через `docker-compose` и `start-services.sh`).  
  - Запустить тесты: `pnpm test:e2e`.

### 10.2. CI-пайплайн

- В репозитории настроен базовый GitHub Actions workflow: `.github/workflows/ci.yml`.  
- При `push`/`pull_request` в `main` автоматически выполняется:  
  - установка зависимостей через `pnpm install --frozen-lockfile`;  
  - запуск `pnpm test` (unit/integration тесты всех сервисов и пакетов).  
- E2E-тесты Playwright пока запускаются вручную (см. 10.1) и могут быть добавлены в отдельный job CI после стабилизации окружения.

### 10.3. E2E тесты для CJM сценариев

В репозитории реализованы автоматизированные E2E тесты для трех основных Customer Journey Map (CJM) сценариев:

#### CJM #1: Родитель - Первый контакт и онбординг
- **Файл:** `apps/web/e2e/cjm.spec.ts`
- **Покрытие:**
  - Вход родителя в систему после приглашения
  - Просмотр списка своих детей
  - Просмотр профиля ребенка с основной информацией
- **Тестовые данные:** `parent1@example.com / parent123`

#### CJM #2: Родитель - Выполнение программы дома
- **Файл:** `apps/web/e2e/cjm.spec.ts`
- **Покрытие:**
  - Просмотр назначенных упражнений
  - Просмотр календаря занятий
  - Просмотр деталей упражнения
  - Отметка выполнения назначения
- **Тестовые данные:** `parent1@example.com / parent123`

#### CJM #3: Нейропсихолог - Полный цикл сопровождения
- **Файл:** `apps/web/e2e/cjm.spec.ts`
- **Покрытие:**
  - Вход специалиста в систему
  - Просмотр списка закрепленных детей
  - Создание диагностической сессии
  - Просмотр доступных опросников
  - Создание индивидуального маршрута
  - Просмотр существующих маршрутов
  - Создание назначения для ребенка
  - Просмотр библиотеки упражнений
  - Просмотр шаблонов программ
- **Тестовые данные:** `specialist1@example.com / specialist123`

#### Сквозные сценарии
- Полный цикл работы специалиста: от диагностики до назначения упражнений
- Просмотр прогресса выполнения программы родителем

#### Запуск CJM тестов

```bash
# Запуск всех E2E тестов (включая CJM)
cd apps/web
pnpm test:e2e

# Запуск только CJM тестов
cd apps/web
pnpm exec playwright test cjm.spec.ts

# Запуск CJM тестов в UI режиме для отладки
cd apps/web
pnpm exec playwright test cjm.spec.ts --ui

# Запуск конкретного CJM сценария
cd apps/web
pnpm exec playwright test cjm.spec.ts -g "CJM #1"
```

#### Структура CJM тестов

Файл `apps/web/e2e/cjm.spec.ts` содержит:
- **Вспомогательные функции:** `clearStorage()`, `loginAs()`
- **3 основных test suite:** CJM #1, CJM #2, CJM #3
- **Дополнительный suite:** Сквозные сценарии
- **Всего тестов:** 14 автоматизированных сценариев

Все тесты используют тестовые данные из `packages/database/prisma/seed.ts` и не требуют дополнительной настройки окружения.


