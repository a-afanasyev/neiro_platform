# План исправления E2E тестов

**Дата:** 2025-12-11
**Цель:** Привести E2E тесты в соответствие с текущей реализацией
**Статус:** План утвержден, готов к выполнению

---

## 📊 Текущая ситуация

- **Всего тестов:** 121
- **Проходит:** 68 (56%)
- **Падает:** 51 (42%)
- **Не запущено:** 2 (2%)

**Основная проблема:** 51 тест из `cjm-extended.spec.ts` проверяют функциональность, которая не реализована.

---

## 🎯 Стратегия исправления

### Вариант 1: Пропустить нереализованные тесты (Рекомендуется)

**Плюсы:**
- ✅ Быстро (1-2 часа работы)
- ✅ Тесты остаются как спецификация будущего
- ✅ CI/CD будут зеленые
- ✅ Можно постепенно включать тесты при реализации

**Минусы:**
- ⚠️ Нужно помнить, какие тесты пропущены

**Реализация:**
```typescript
// В cjm-extended.spec.ts
test.skip('CJM #4.1: ...', async ({ page }) => {
  // TODO: Реализовать /dashboard/specialist/
})
```

### Вариант 2: Разделить тесты по папкам

**Плюсы:**
- ✅ Четкое разделение реализованного и планируемого
- ✅ Легко управлять в CI/CD
- ✅ Понятная структура

**Минусы:**
- ⚠️ Нужно создать отдельный конфиг Playwright
- ⚠️ Больше времени на настройку

**Структура:**
```
e2e/
  ├── current/              # Текущие тесты (запускаются всегда)
  │   ├── auth.spec.ts
  │   ├── children-crud.spec.ts
  │   ├── users-crud.spec.ts
  │   ├── dashboard.spec.ts
  │   ├── parent-management.spec.ts
  │   ├── notifications.spec.ts
  │   ├── progress.spec.ts
  │   ├── reports.spec.ts
  │   └── cjm-basic.spec.ts
  └── future/               # Будущая функциональность (не запускаются)
      └── cjm-extended.spec.ts
```

### Вариант 3: Адаптировать тесты под текущую реализацию

**Плюсы:**
- ✅ Максимальное покрытие текущего функционала

**Минусы:**
- ⚠️ Много работы (2-3 дня)
- ⚠️ Потеря спецификации будущего функционала
- ⚠️ Придется переписывать тесты при реализации CJM

---

## ✅ Выбранная стратегия: Вариант 1 + Частично Вариант 3

**План:**
1. Пропустить все тесты CJM #4-#12 (нереализованные)
2. Исправить частично работающие тесты (CJM #1-#3, Dashboard, etc.)
3. Добавить комментарии с TODO для будущей реализации
4. Создать ROADMAP с планом реализации

---

## 📋 План действий (пошагово)

### Шаг 1: Аудит текущих тестов (30 мин)

Пройти по каждому тестовому файлу и определить статус:

| Файл | Статус | Действие |
|------|--------|----------|
| auth.spec.ts | ✅ 100% pass | Оставить как есть |
| children-crud.spec.ts | ✅ 100% pass | Оставить как есть |
| users-crud.spec.ts | ⚠️ Частично | Исправить селекторы |
| dashboard.spec.ts | ⚠️ Частично | Исправить ожидания |
| parent-management.spec.ts | ⚠️ Частично | Исправить проверки |
| notifications.spec.ts | ⚠️ Частично | Адаптировать под API |
| progress.spec.ts | ⚠️ Частично | Проверить данные |
| reports.spec.ts | ⚠️ Частично | Исправить загрузку |
| cjm.spec.ts | ⚠️ Частично | Исправить базовые CJM |
| cjm-extended.spec.ts | ❌ 0% pass | Пропустить все тесты |

### Шаг 2: Исправить cjm-extended.spec.ts (1 час)

**Действия:**
1. Добавить `test.skip()` ко всем тестам CJM #4-#12
2. Добавить комментарии с описанием недостающей функциональности
3. Добавить ссылки на issues/tasks в Jira/GitHub

**Код:**
```typescript
test.describe.skip('CJM #4: Профильный специалист', () => {
  /**
   * TODO: Реализовать функциональность профильного специалиста
   *
   * Требуется:
   * - Страница /dashboard/specialist/
   * - Карточки детей с целями
   * - Заметки от нейропсихолога
   * - Расписание занятий
   * - Таймер для занятий
   * - Система отчетов
   *
   * Issue: https://github.com/org/repo/issues/123
   * Milestone: Month 4
   */

  test('CJM #4.1: Получение назначения', async ({ page }) => {
    // ... тест остается для будущего
  })

  // ... остальные тесты
})
```

### Шаг 3: Исправить частично работающие тесты (2-3 часа)

#### 3.1 users-crud.spec.ts

**Проблемы:**
- Некорректные селекторы
- Неверные ожидания

**Исправления:**
```typescript
// Было
await page.click('text=Создать пользователя')

// Стало
await page.click('[data-testid="create-user-button"]')
// ИЛИ
await page.click('button:has-text("Создать пользователя")')
```

#### 3.2 dashboard.spec.ts

**Проблемы:**
- Тесты ожидают элементы, которых нет

**Исправления:**
- Проверять только реализованные разделы
- Убрать проверки несуществующих элементов

#### 3.3 cjm.spec.ts (базовые CJM)

**Проблемы:**
- Некоторые сценарии частично реализованы

**Исправления:**
- CJM #1: Оставить как есть (работает)
- CJM #2: Исправить проверки назначений
- CJM #3: Адаптировать под текущие страницы

### Шаг 4: Добавить data-testid в UI компоненты (1-2 часа)

**Цель:** Сделать тесты более надежными

**Места для добавления:**
- Кнопки действий (создать, редактировать, удалить)
- Формы
- Навигационные элементы
- Модальные окна

**Пример:**
```tsx
// Было
<Button onClick={handleCreate}>Создать</Button>

// Стало
<Button onClick={handleCreate} data-testid="create-user-button">
  Создать
</Button>
```

### Шаг 5: Обновить seed данные (30 мин)

**Проблема:** Тесты могут падать из-за отсутствия нужных данных

**Действия:**
1. Проверить seed.ts
2. Убедиться, что есть:
   - Пользователи всех ролей (admin, specialist, parent)
   - Дети с назначенными специалистами
   - Маршруты и упражнения
   - Тестовые назначения

### Шаг 6: Создать ROADMAP.md (30 мин)

**Содержание:**
- График реализации CJM #4-#12
- Приоритеты функциональности
- Зависимости между модулями
- Оценки времени

---

## 📝 Детальные исправления по файлам

### cjm-extended.spec.ts

```typescript
import { test, expect } from '@playwright/test'

// ============================================
// CJM #4: ПРОФИЛЬНЫЙ СПЕЦИАЛИСТ
// ============================================

test.describe.skip('CJM #4: Профильный специалист', () => {
  /**
   * ⚠️ ФУНКЦИОНАЛЬНОСТЬ НЕ РЕАЛИЗОВАНА
   *
   * Этот блок тестов пропущен, так как требует реализации:
   * 1. Специализированной панели специалиста (/dashboard/specialist/)
   * 2. Расширенных функций для профильных специалистов
   * 3. Системы расписания и проведения занятий
   *
   * Приоритет: Month 4
   * Оценка: 2-3 недели разработки
   *
   * Зависимости:
   * - Базовые CJM #1-#3 должны быть реализованы
   * - Требуется дизайн UI для панели специалиста
   * - Требуется API для расписания занятий
   *
   * @see https://github.com/org/repo/milestone/4
   */

  test.beforeEach(async ({ page }) => {
    await clearStorage(page)
  })

  test('CJM #4.1: Получение назначения - Понять задачу', async ({ page }) => {
    // Тест сохранен как спецификация
    await loginAs(page, 'specialist2@example.com', 'admin123')
    await page.click('text=Уведомления')
    await expect(page.locator('text=Новое назначение')).toBeVisible({ timeout: 5000 })
    await page.click('text=Панель специалиста')
    await expect(page).toHaveURL(/\/dashboard\/specialist/)
    await expect(page.locator('text=Цели ребенка')).toBeVisible({ timeout: 5000 })
    await expect(page.locator('text=Заметки от нейропсихолога')).toBeVisible({ timeout: 5000 })
  })

  // ... остальные тесты CJM #4
})

// Аналогично для CJM #5, #6, #7, #8, #8a, #9-12
test.describe.skip('CJM #5: Супервизор', () => { /* ... */ })
test.describe.skip('CJM #6: Администратор / организация', () => { /* ... */ })
test.describe.skip('CJM #7: Ребенок - Игровой опыт', () => { /* ... */ })
test.describe.skip('CJM #8: Telegram/PWA взаимодействия', () => { /* ... */ })
test.describe.skip('CJM #8a: Методист / контент-куратор', () => { /* ... */ })
test.describe.skip('CJM #9-12: Расширенные профессиональные сценарии', () => { /* ... */ })
```

### dashboard.spec.ts

**Исправления:**
```typescript
test('should display admin dashboard', async ({ page }) => {
  await loginAs(page, 'admin@example.com', 'admin123')

  // Проверяем только реализованные элементы
  await expect(page).toHaveURL('/dashboard')

  // Было: await expect(page.locator('text=Панель администратора')).toBeVisible()
  // Стало: Проверяем навигацию
  await expect(page.locator('[data-testid="users-menu"]')).toBeVisible()
  await expect(page.locator('[data-testid="children-menu"]')).toBeVisible()
  await expect(page.locator('[data-testid="analytics-menu"]')).toBeVisible()

  // Убираем проверки несуществующих элементов:
  // ❌ await expect(page.locator('text=Статистика организации')).toBeVisible()
  // ❌ await expect(page.locator('text=KPI специалистов')).toBeVisible()
})
```

---

## 🚀 Порядок выполнения

### День 1 (4 часа)

**Утро:**
- [ ] Шаг 1: Аудит всех тестов (30 мин)
- [ ] Шаг 2: Пропустить CJM Extended тесты (1 час)
- [ ] Commit: "test: skip unimplemented CJM Extended tests"

**День:**
- [ ] Шаг 3.1: Исправить users-crud.spec.ts (1 час)
- [ ] Шаг 3.2: Исправить dashboard.spec.ts (1 час)
- [ ] Запустить тесты, проверить прогресс
- [ ] Commit: "fix: update dashboard and users CRUD tests"

### День 2 (4 часа)

**Утро:**
- [ ] Шаг 3.3: Исправить cjm.spec.ts (1 час)
- [ ] Шаг 3.4: Исправить parent-management.spec.ts (1 час)
- [ ] Commit: "fix: update CJM basic and parent management tests"

**День:**
- [ ] Шаг 4: Добавить data-testid (1.5 часа)
- [ ] Шаг 5: Проверить seed данные (30 мин)
- [ ] Запустить все тесты
- [ ] Commit: "test: add data-testid attributes for better test reliability"

### День 3 (2 часа)

**Финализация:**
- [ ] Шаг 6: Создать ROADMAP.md (30 мин)
- [ ] Запустить все тесты финально (30 мин)
- [ ] Обновить TEST_EXECUTION_PLAN.md (30 мин)
- [ ] Создать PR с описанием изменений (30 мин)

---

## 📊 Ожидаемые результаты

### До исправлений:
```
Всего тестов:  121
Пройдено:      68  (56%)
Провалено:     51  (42%)
Не запущено:   2   (2%)
```

### После исправлений:
```
Всего тестов:  121
Пройдено:      68-75 (56-62%)
Провалено:     0-5   (0-4%)
Пропущено:     46-53 (38-44%)
```

### Цель:
- ✅ Все реализованные тесты проходят (100%)
- ✅ Нереализованные тесты пропущены с комментариями
- ✅ CI/CD зеленые
- ✅ Тесты служат спецификацией для будущей разработки

---

## 📁 Создаваемые документы

1. **E2E_TESTS_FAILURE_ANALYSIS.md** ✅ Создан
   - Детальный анализ причин падения тестов

2. **E2E_TESTS_FIX_PLAN.md** ✅ Текущий документ
   - План исправления тестов

3. **ROADMAP.md** ⏳ Создать
   - Roadmap реализации CJM #4-#12

4. **TEST_EXECUTION_PLAN.md** ✅ Обновить
   - Обновить статусы и результаты

---

## ✅ Чек-лист завершения

### Технические задачи:
- [ ] Пропущены все тесты CJM #4-#12
- [ ] Исправлены частично работающие тесты
- [ ] Добавлены data-testid в критичные компоненты
- [ ] Проверены seed данные
- [ ] Все тесты запускаются без ошибок
- [ ] CI/CD настроен и проходит

### Документация:
- [ ] Создан ROADMAP.md
- [ ] Обновлен TEST_EXECUTION_PLAN.md
- [ ] Добавлены комментарии в тестах
- [ ] Созданы issues для нереализованной функциональности

### Коммуникация:
- [ ] Создан PR с описанием
- [ ] Команда уведомлена о статусе
- [ ] Приоритеты согласованы с product owner

---

## 🎯 Следующие шаги после исправления

1. **Month 4: Реализация CJM #4**
   - Панель профильного специалиста
   - Расписание занятий
   - Система отчетов

2. **Month 5: Реализация CJM #5**
   - Панель супервизора
   - Аналитика команды
   - Контроль качества

3. **Month 6: Реализация CJM #6**
   - Административная панель
   - Управление организацией
   - KPI и аналитика

4. **Month 7-9: CJM #7-#12**
   - Игровой режим для детей
   - PWA/Telegram интеграция
   - Панель методиста
   - Система обучения

---

## 📞 Контакты и поддержка

**Вопросы по тестам:** @qa-team
**Вопросы по приоритетам:** @product-owner
**Технические вопросы:** @tech-lead

**Документация:**
- [E2E_TESTS_FAILURE_ANALYSIS.md](./E2E_TESTS_FAILURE_ANALYSIS.md)
- [TEST_EXECUTION_PLAN.md](./TEST_EXECUTION_PLAN.md)
- [Neiro_CJM_Extended.md](./Documents/Neiro_CJM_Extended.md)
